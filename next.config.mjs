/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.shutterstock.com",
      "img.freepik.com",
      "st3.depositphotos.com",
    ],
  },
  output: 'export',
};

export default nextConfig;
